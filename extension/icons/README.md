# Extension Icons

The PNG icon assets for the extension are generated locally to avoid committing binary files.

Run `./build.sh` from the repository root to recreate the icons (`icon16.png`, `icon48.png`, `icon128.png`). The script will place the generated files in this directory.
